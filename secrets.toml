# .streamlit/secrets.toml
# Used by Streamlit Cloud (and local dev if you prefer secrets over .env)
#
# LOCAL:  create this file at .streamlit/secrets.toml — git-ignored automatically
# CLOUD:  paste contents into Streamlit Cloud → App Settings → Secrets
#
# DO NOT commit this file to Git.

BACKEND_URL = "https://your-app-name.onrender.com"

# Optional: pre-fill a default Groq key so users don't have to enter one
# GROQ_API_KEY = "gsk_..."
